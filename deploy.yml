name: Deploy Internal Chatbot

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build Docker Image
        run: docker build -t internal-chatbot .

      # Example for Azure App Service / ACR
      # Replace with your platform
